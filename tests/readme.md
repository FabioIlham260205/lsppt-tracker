```
npx playwright test                              # Run all
npx playwright test tests/submit.spec.js         # Submit only
npx playwright test tests/history.spec.js        # History only
npx playwright test tests/full-flow.spec.js      # Full flow only
```